#include " ft_printf.h"


static ft_con(const char type, va_list)
{

}

int ft_printf(const char *str, ...)
{
	va_list	args;
	int		i;
	va_start(args, str);
	while (str[i] != "\0")
	{
		if (str[i] == "%")

	}


	return (i);
}
